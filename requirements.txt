# Framework自动化构建工具依赖

# 核心依赖
click>=8.0.0          # 命令行工具
jinja2>=3.0.0         # 模板引擎
pyyaml>=6.0           # YAML配置文件支持
colorama>=0.4.4       # 彩色输出

# 文件处理
pathlib2>=2.3.6      # 路径处理
shutil>=3.8.0         # 文件操作

# 文档生成
markdown>=3.4.0       # Markdown处理
pygments>=2.10.0      # 代码高亮

# 项目文件生成
uuid>=1.30            # UUID生成
datetime>=4.3         # 时间处理

# 可选依赖 (用于高级功能)
# requests>=2.28.0    # HTTP请求 (用于上传到远程仓库)
# gitpython>=3.1.0    # Git操作 (用于版本管理) 